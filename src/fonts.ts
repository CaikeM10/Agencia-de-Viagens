// Fontes de letras que seram implementadas
import { Playfair_Display } from "next/font/google";

export const titleFont = Playfair_Display({
  subsets: ["latin"],
  weight: ["700"], // Escolha o peso que você precisa
  display: "swap",
});
